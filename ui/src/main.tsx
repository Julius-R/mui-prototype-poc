import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

/*
Elements to add for now
- Button
- Grid
- Container
- Card
- Box
 */

createRoot(document.getElementById('root')!).render(<StrictMode></StrictMode>)
